// Verificar Nulidad
function verificarNulidadDeValor(val) {
    return (val == null || val == undefined || val == "");
}

function cantidadDeCaracteres(valorInput, CantidadDeCaracteres) {
    return (valorInput.length >= CantidadDeCaracteres);
}